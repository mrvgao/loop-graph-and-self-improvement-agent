# 17 个演示分别在讲什么

每个演示一张「场景卡」：**什么场景 · 机制是什么 · 关键在哪 · 看板看哪一栏 · 排练数字 · 要讲的那句话**。
数字是 2026-09-05 用课上设置（`claude-sonnet-5`，标明的三处用 `claude-haiku-4-5`）真跑出来的。
配合走场单 `../SCRIPT.md` 和演示表 `README.md` 使用。

| | 文件 | 一句话 |
|---|---|---|
| A0 | `a0_self_driving.py` | 人写五行，程序自己把一个空仓库做到测试全绿 |
| A1 | `a1_verify_retry.py` | 一次性调用就是 K=1 的循环 |
| A2 | `a2_stop_resume.py` | 循环什么时候停，停了怎么免费接着走 |
| A3 | `a3_prompt_evolution.py` | 让循环改写 prompt，然后看它学到的是习惯还是答案 |
| B0 | `b0_planned_graph.py` | 没有人画图：planner 自己吐出图的规格 |
| B1 | `b1_cycle_cap.py` | 带上限的环，以及噪声评委 |
| B2 | `b2_fanout_gate.py` | 并行审稿 + 进程可以走开的人类闸 |
| C1 | `c1_best_of_n.py` | 多试几次买到的是覆盖率，挑法才是尺子 |
| C2 | `c2_tree_search.py` | 树比线多买到「能回到更早更好的节点」 |
| C3 | `c3_reflexion.py` | 语言当梯度的最小版本 |
| C4 | `c4_ace_playbook.py` | 增量 playbook vs 整段重写 |
| C5 | `c5_skill_library.py` | 技能库的复利 |
| C6 | `c6_evolve_program.py` | 进化一个构造程序的源码 |
| C7 | `c7_self_modify.py` | agent 改写自己的 `solve()` |
| C8 | `c8_harness_evolution.py` | 先长出来的不是能力，是裁判 |
| C9 | `c9_ruler.py` | 同一个优化器，两把尺，骗子与泛化者 |
| C10 | `c10_star_bootstrap.py` | 自己教自己：拒绝采样到训练集 |

---

# A 组 · 把循环拆成零件

## A0 自驱环 · `a0_self_driving.py`

**场景**　一个空的小仓库：六个函数只有 `raise NotImplementedError`，29 条测试全红，PLAN.md 上六个空勾。人写五行驱动词，写一次，之后再不碰。程序自己跑到全绿。

**机制**　每一轮是一个**全新的操作系统进程**，上下文从空开始。agent 有五个工具：列目录、读文件、写文件、跑测试、看 git log。它自己去读 SPEC、PLAN、PROGRESS、git log、代码、测试。**它读到的东西，就是它这一轮的 prompt。** 然后挑一项未完成任务、写代码、跑测试、打勾、往 PROGRESS 追加一行、退出。harness 替它 commit，再换**另一个模型**判「整体做完了没有」。

**关键**　上下文不放在对话里，放在磁盘上。所以进程可以随便死，状态还在；也不会越滚越脏、越滚越贵。做题的不给自己打分：它每轮都说 DONE，只有裁判说的那次算数。

**看板**　LLM 调用面板点开任意一轮的最后一次调用，五行变成几千 token。上下文曲线一轮之内锯齿上升（每读一个文件长一截），跨轮归零重来但起点更高，因为它在读自己上一轮写的东西。

**数字**　6 轮到 goal，测试 0→5→9→16→20→25→29/29，每轮平均上下文 2.5k→4.1k tok，54 次调用，189 秒，$0.62。

> prompt 是这个循环的产物，不是它的输入。

## A1 验证-重试 · `a1_verify_retry.py`

**场景**　六道小题，比如把 `'10%'` 变成 `0.1`、把 `'1h30m'` 变成 5400 秒。三条臂跑同样的题，**是同一个函数**，只差传进去的参数。

**机制**　A 试 1 次。B 试 4 次但不看反馈，只把温度调到 0.8 重掷骰子。C 试 4 次，并把测试那句话拼回输入。题目的 spec 故意写不全，隐藏用例是真实用户忘了写下来的规则（带逗号的百分数、大写的 `1H30M`、比宽度还长的单词、`v` 前缀），猜不到，只能测出来。失败时尺子只回一句话：输入、期望、实际。

**关键**　B 是对照组，别跳过。A 到 C 一次动了两个变量（次数 1→4、反馈 无→有），没有 B 就无法归因，「你不就是多调了四次模型吗」这句反驳就站得住。另外 B 的第一次尝试和 A 是同一次调用，所以 B 把 A 包住了。

**看板**　分数面板三根柱；时间线上 B 那几行的停机原因；LLM 调用里 arm C 第二次调用多出来的那一句。

**数字**

| 臂 | 解决 | 每解决一题的 token |
|---|---|---|
| A（K=1） | 2/6 | 674 |
| B（K=4 盲重试） | 2/6 | 1078 |
| C（K=4 验证-重试） | 6/6 | 688 |

多试几次买到 +0，把尺子那句话放进输入买到 +4。B 有 4/6 题停在 `no-progress`：温度 0.8 也写出了**字节相同**的代码，省下 8 次调用。

> 循环不是你加的功能，是你一直设成 1 的那个参数。

## A2 停机续跑 · `a2_stop_resume.py`

**场景**　一批活：12 条客服留言，每条抽成一个 JSON。这是最常见的 agent 形态，批处理。

**机制**　三样东西。停机条件是一个**有序列表**，而且在每步**之前**查：步数、token、墙钟；顺序就是你想听到的顺序。每做完一条就存盘，临时文件加 `os.replace`，是原子的，崩在写一半也不会留下半个文件；存盘的键是条目编号，所以续跑幂等、零重复。无进展规则：内层校验最多试两次，两次都不对就跳过、记原因、往下走。

**关键**　为什么必须在花钱之前查预算？事后查恒定超一步，而最后那一步往往最贵。为什么必须有无进展规则？不然十二条的活会变成一条的账单。

**看板**　崩溃那条跑完，左上角的点变红「连接断开」；第二条起来，横幅「已重连到新进程（续跑）」，时间线从第 6 条接着走。第三条：预算条涨到 80% 变红，停机标签 `token_budget`。

**数字**　三条命令：崩溃（5 条后 `os._exit(137)`）、续跑（7 次调用零重复）、预算停机（800 tok 停在剩 5 条）。合计约 $0.02。

> `os._exit` 就是 `kill -9`，连输出缓冲一起丢。循环死得硬的时候，它最后说的那句话是你最先丢的。检查点存在，是因为日志活不下来。

## A3 让循环改写 prompt · `a3_prompt_evolution.py`

**场景**　把 A1 的内层重试**关掉**（K=1，逼 prompt 自己扛能力），外面套一层循环。被改的东西 θ 是 system prompt 本身。

**机制**　Propose：反思模型读失败那几句话，把 prompt 改写一版。Evaluate：用三道**公开**题打分。Select：只在公开分涨时接受。Retain：归档，被拒的下次给反思模型看，免得重复。另外三道**私有**题算了分、记了分，**从不给 Propose 看**。

**关键**　两条曲线一起读。public 涨且 private 涨，是学到了习惯；public 涨而 private 不动，是学到了答案。同一个循环，只有第二列能告诉你是哪一种。最终 prompt 里常常有一行把隐藏用例逐字抄了进去。

**看板**　Prompt 面板 v0 到 v3，接受的实心、拒绝的空心，并排 diff 看它往 prompt 里写了什么；分数面板两条线；验证表筛「私有」。

**数字**　3 代，接受 1/3，27 次调用，94 秒，$0.10。温度 0.7，每次结果不同。

> 循环没有错，它精确地做了你的 Select 奖励的事。这不是 bug，是 C 部分的主题。

---

# B 组 · 把循环推广成图

循环是只有一个节点、一条回边的图。循环体一旦有两**种**步（写和评、计划和审），你就已经在画图了，只是没起名字。起了名字才买得到四件事：每个节点都能**存档、中断、续跑、审计**。

## B0 没有人画图 · `b0_planned_graph.py`

**场景**　给一个目标（写代码，或写一份简报）、一份节点目录、一份 contract（每种节点读写哪些状态键），让 planner 模型自己吐出一张图的 JSON 规格；运行时校验、编译进 tinygraph、跑。

**机制**　planner 能定节点、角色、连线。两件事它碰不了：目标指定的**锚点校验器**必须在每条通向结束的路上，且参数冻结；每个环必须经过 router，上限是运行时的。违规就拒绝，拒绝信原句还给 planner 重来。所以 planner 自己也在一个验证-重试环里。

**关键**　同一个运行时、两个目标，长出两张不同的图。图是数据，不是代码。

**看板**　Prompt/产物面板每次尝试一版规格，被拒的空心加拒绝理由；图面板两个 tab，锚点节点紫色双边，router 的条件边带 pass/fail/cap 标签。

**数字**　两个目标都过了锚点，5 次调用，20 秒，$0.02。

> planner 可以提任何图；它不能提的那两样东西，正是让结果可信的那两样东西。

## B1 带上限的环 · `b1_cycle_cap.py`

**场景**　写一段 90 到 120 词的招生文案。一个模型写，另一个模型当编辑，打分并列出违规项。

**机制**　四个节点：draft → critique → 要么 accept 结束、要么 revise 再绕回 critique。全部工程在路由那三行：分数够就接受；改够三次就放弃；否则继续改。

**关键**　评委也是模型，所以它是一把**有噪声的尺子**。画成图不会让它变准，只会让噪声有界、可见：分数进状态，走过的路径打印出来。上限是这个环唯一保证会终止的东西；没有它，一把噪声尺子的寿命就是这个循环的寿命。

**看板**　图里 revise→critique 的回边加粗、当前节点脉冲、下方执行路径 chips；分数面板逐轮的评分；Prompt 面板每版 draft 的 diff，看评委列的违规项有没有真被修掉。

**数字**

| 设置 | 结果 |
|---|---|
| `--pass 9`（默认） | 8, 8, 9 分，改两次后接受 |
| `--pass 8` | 一稿就过，环一次都不转 |

6 次调用，20 秒，$0.02。

> 评委松一格，整个环就消失了。要不要循环不是架构决定的，是尺子的严格程度决定的。

## B2 扇出扇入 + 人类闸 · `b2_fanout_gate.py`

**场景**　写一份部署方案，三个审稿人分别从安全、成本、回滚三个角度并行审，意见合并，然后**等人批准**才发布。

**机制**　两个新零件。扇出：三个节点拿同一份状态的副本并行跑，结果合并回来；`findings` 声明成 append 型的键，所以三份意见是拼接，不是最后写的人覆盖前面。人类闸：`interrupt_before=["human_gate"]`，运行时走到这个节点之前把状态存盘，然后直接 return，**进程退出**。没有任何线程在等 `input()`。

**关键**　第二个进程 `--approve` 从检查点续跑，账单是 **0 次调用**，审稿人不重付。`--reject "理由"` 则把下一个节点指回 plan，同时用 `Replace()` 把 findings 清空：append 型的 reducer 必须有办法被告知「这轮重来」，否则上一轮的意见会一直粘着。

**看板**　图里三个审稿人同时脉冲，human_gate 虚线红框，停机标签 `interrupted before human_gate`；第二个进程横幅「已重连到新进程（续跑）」，Token 表 0 次调用。

**数字**　第一个进程 4 次调用出 9 条意见，第二个进程 0 次调用，合计 $0.02。

> 等人的代价是一个 JSON 文件，不是一个抱着上下文的活进程。人十秒回也行，十天回也行，中间机器可以关掉。

---

# C 组 · 把同一个循环往上套一层

C 组十个演示是同一个骨架：**Propose 提候选 · Evaluate 打分 · Select 留下 · Retain 归档**。A3 就是它，只不过 θ 是 prompt。区别只在被改的东西是什么：上下文、prompt、技能、程序、harness、还是 agent 自己的源码。这就是那把阶梯。

## C1 多试几次 · `c1_best_of_n.py`

**场景**　一道题采 6 个样本，四种「从 6 个里挑一个交卷」的策略比高低。题目来自难题库：正则引擎、电子表格、CSV 解析、semver 范围、大数加法、拓扑序，规格写全了但要写六十到一百五十行才做得对，有的题还禁用现成库。

**机制**　`first` 交第一个。`majority` 让每个候选跑一遍探针输入，按输出向量聚类，交最大簇的代表，**只用输入不用答案**。`verifier` 交第一个通过公开测试的。`oracle` 只要有一个过隐藏测试就算过，这是上界。pass@k 用 Chen 的无偏估计算。

**关键**　采样模型故意用 haiku。sonnet 在这套题上 pass@1 就是 0.97，四种挑法全 6/6，曲线是一条平线。这本身就是结论。

**数字**　haiku：pass@1 0.47 → pass@6 1.00；first 2 / majority 3 / verifier 3 / oracle 6（满分 6）。36 次调用，46 秒，$0.17。sonnet：0.97 → 1.00，全 6/6，$0.60。

> 多试几次买到的是覆盖率；把覆盖率变成正确率靠的是挑法，而挑法就是尺子。oracle 和 verifier 之间的差，就是一把更好的尺子的价钱。

## C2 树搜索 · `c2_tree_search.py`

**场景**　一次要写三个难函数（正则引擎、电子表格、大数加法）的完整模块。A1 的重试是一条线，这里是一棵树。

**机制**　状态是一整个模块，分数有三十多级不是 0/1。每轮从 frontier 里按（dev 分数，深度）取最好的两个节点，各扩两个孩子：`fix` 温度 0.3，只修失败用例别动其它；`rethink` 温度 0.9，按 spec 重写失败的函数。一轮四个孩子并行生成。dev 集（公开 + 一半隐藏）用来搜索，holdout（另一半）只汇报不参与选择。

**关键**　树比线多买到的是**能回到更早、更好的节点再扩**。线性重试做不到这第二件事。

**数字**　haiku：根 2/18 → n1 14/18 → 四个孙子 10 到 11，都没超过 n1，最优停在更早的节点。7 次调用，55 秒，$0.15。sonnet：根 5/18（它 import 了被禁的 `re`，尺子当场抓住）→ 一步 fix 就 18/18，树只有三个节点。

> 同样十几次调用，线性重试只能修一条线上的错。

## C3 Reflexion · `c3_reflexion.py`

**场景**　还是那六道题，但失败之后多一步：让模型对着一份「宪法」写一段反思，存进记忆；下一次尝试带着**全部**反思再来。

**机制**　宪法是五条原则：例子就是规格，要说出规则而不是例子；真实输入是脏的；一条失败用例代表一**类**输入；优先最小的改动；把教训写成陌生人也能套用的话。反思是文字不是梯度，跨试次累积而不是每次清零。

**关键**　什么时候自评可信？验证比生成容易的时候。所以这里评的不是模型对自己的看法，而是尺子那句话加上原则。`--no-reflect` 是对照组，退回成 A1 的盲重试。

**数字**　6 题，每题最多 3 次，解决 5/6，18 次调用，53 秒，$0.04。

> 「语言当梯度」的最小可运行版本。

## C4 ACE · `c4_ace_playbook.py`

**场景**　17 条客服工单要路由到 7 个乱起名的团队（ORCA、VESTA、LUMEN、ATLAS、PINE、KESTREL、REDLINE）。规则只存在于数据里，猜不到，只能从错例学。里面还埋了两条覆盖规则：紧急加账单归 REDLINE（压过 LUMEN），凡涉及 API 归 KESTREL（压过一切）。

**机制**　两条臂从同一句种子指令出发。ACE 臂三个角色：Generator 拿着 playbook 去路由，Reflector 读这一批的错例写教训，Curator 把教训变成对**编号条目**的增删改（delta），playbook 只累积、绝不整段重写。对照臂拿同样的错例，让模型把整个 prompt 重写一遍，这是大多数人的做法。在线学习：每轮只看 5 条的小批量，但全集评估。

**关键**　准确率两臂常常打平，真正分开它们的是写出成本和可审计性。ACE 每轮只写 Θ(Δ)，重写臂每轮烧 Θ(整个上下文)，而且随上下文增长。ACE 的每条规则有出处，可以逐条否决。

**数字**　3 轮：ACE 6→7→15/17，重写臂 9→12→12/17。写出字符：ACE 3642、重写 2665。145 次调用，279 秒，$0.11。

**诚实说**　这次 ACE 写出的字符**没有**少于重写臂（策展模型把 delta 写得太啰嗦），它这轮赢的只是可审计。论文说的「上下文塌缩」在玩具规模也复现不出来。只卖数据能证明的东西。

## C5 技能库 · `c5_skill_library.py`

**场景**　五个递进的 CSV 小任务。每个任务写出来的函数如果自测通过就进库，后面的任务可以**直接调用**库里的技能，不用重新实现。

**机制**　检索相关技能 → 写函数（可以调用库里的）→ 跑自测 → 失败就把报错喂回去修，最多 4 次 → 通过就起名入库。数据里埋了一个环境惊喜：数值列里混着 `"10%"` 这样的字符串，只有读了报错才会发现。

**关键**　技能是软基底里唯一**可执行、可测试、可组合**的进化单元，它的自验证就是它的验收测试。学一次的 `parse_csv`，后面三个技能都在调用它。阴面：过拟合测试的技能会被反复复用。

**看板**　`library.py` 每入库一版，diff 就是新技能；点开第四个任务的 LLM 调用，上下文里「你可以直接调用的技能」那份列表在变长。

**数字**　5 个技能全部入库，5 次调用，16 秒，$0.01。

## C6 进化程序 · `c6_evolve_program.py`

**场景**　把 8 个圆装进一个单位正方形，不许重叠，让半径之和最大。种子是 3×3 网格去掉一个角，半径 1/6，得分 1.3333。

**机制**　循环**从不直接动坐标**，它进化的是一个 `pack()` 构造函数的**源码**，改一处代码八个圆一起动。评估器是纯 Python，跑在子进程里 2 秒超时：是不是 8 个圆、半径是否为正、有没有出界、有没有重叠；合法就算半径和，不合法给 0 分外加一句原因。归档存下每个评估过的程序、分数和父节点；亲本按排名加权从前四里抽（4:3:2:1）。5 代，每代 5 个孩子并行。

**关键**　稀疏成功。大多数变异不合法或更差；大跳跃来自换构造策略（比如允许不等半径、一大四小），不是调参数。

**看板**　`pack.py` 每出一个新最优就是一版，diff 是策略级的改写；旁边直接把当前最优的八个圆画出来。

**数字**　1.3333 → 1.3539。25 次变异里：不合法 13、更差 10、更好 2。100 秒，$0.52。

> 预算是零件，不是选项。

## C7 改自己代码的 agent · `c7_self_modify.py`

**场景**　一个 agent 的 `solve()` 只有三行：问一次模型、把代码抠出来、返回。让 meta 模型改写这个函数的**源码**，看它会进化成什么。

**机制**　改进者和被改进者是同一个对象。`solve()` 是一个源码字符串，被 exec 进一个**受限命名空间**，只能用四样东西：`llm_write`（每题最多 6 次，跑飞了就掐）、`run_public_tests`（允许自验证，隐藏尺子它拿不到）、`extract_code`、`task`。基准是六道题过隐藏测试的数量。每代 meta 模型看源码、每题失败的尺子句子、工具契约，改写 `solve()`；**严格高于亲本才接受**，全部候选归档。

**关键**　它加的东西全是 harness 零件：重试、把报错喂回去、自测、反思。因为「一个更会写代码的 agent」需要的正是这些，所以改进落回到了改进者自身。DGM 论文的发现在你眼前长出来。

**看板**　`solve.py` v0 到 v4，接受的实心；并排 diff 看 for 循环、报错回流、自测一样一样长出来。

**数字**　v0 1/6 → v1 5/6 → v2 6/6；v3、v4 写出语法错误被拒。22 次调用，66 秒，$0.12。

## C8 进化 harness · `c8_harness_evolution.py`

**场景**　一条两节点的流水线：reader 读 changelog，writer 写发布说明。种子规格**故意写得松**：「总结一下」「写个友好的说明」，像大多数人的第一版 prompt。

**机制**　五个**确定性**的缺陷探测器每次运行后打分：D1 结构是不是带 type/text/issue 的 JSON 数组；D2 changelog 里每个已发布的 issue 号是不是都出现在说明里；D3 是不是 150 词以内；D4 有没有 Added 和 Fixed 两个标题；D5 说明里有没有 changelog 里**不存在**的 issue 号，也就是幻觉。每代把缺陷清单（一句一句，荒唐地具体）交给 meta 模型，它可以改角色、改 contract、**加裁判节点**、加 hop。运行时校验规格：未知节点类型、悬空目标、没上限的环，一律拒绝并把拒绝信还回去。严格更少缺陷才接受，而且**只跟上一版比**。

**关键**　先长出来的不是任务能力，是裁判。

**看板**　图面板每代一个 tab，新长出的 validator 节点紫色双边；`harness.json` 的 diff 就是新节点和新 hop。

**数字**　v0 缺陷 8（裁判 0，hop 1）→ v1 缺陷 0（裁判 5，hop 11）ACCEPT。34 次调用，57 秒，$0.12。

> 自进化系统最先学会的不是把活干好，是不再信任自己。

## C9 尺子研究 · `c9_ruler.py`

**场景**　给 1 到 30 的整数贴 True/False。隐藏规则是「能被 3 整除」，但故意加了三个例外（4 是真、9 是假、22 是真），让「背下公开集」在公开集上比「找到规则」更划算。

**机制**　agent 的 harness 只有两个旋钮：`divisor`（规则假设）和 `overrides`（逐条硬编码，也就是记忆容量）。一个**确定性的假提案器**每轮既提「换 divisor」，也提「把公开集里错的几条硬编码进去」，也就是作弊。同一个优化器跑两遍，**只换评委看的集合**。

**关键**　零次模型调用，零成本。这一课的道理不需要花钱。

**数字**

| 评委看的集合 | 最后长成什么 | public | private |
|---|---|---|---|
| LEAKY（公开集） | divisor=2 加 4 条硬编码 | 1.00 | 0.60 |
| PRIVATE（私有集） | divisor=3 | 0.80 | 0.95 |

> 优化器一模一样，只有尺子不同。agent 会长成你用来量它的那把尺子的形状。A3 在真模型上三代就到了同一个地方。

## C10 自己教自己 · `c10_star_bootstrap.py`

**场景**　16 道组合与数论小题，加 8 道 holdout（答案全部用代码复核过，`--selftest`）。学生模型是 haiku，先裸跑一遍，答对的那些推理留下来写成 `dataset.jsonl`，然后用它再学一遍。

**机制**　拒绝采样：只留答案正确的推理。合理化：答错的题，把正确答案当提示让它写出到达答案的推理，也留下。第二遍：用留下的推理做 few-shot 再跑，leave-one-out，绝不把本题的推理喂给本题。

**诚实声明**　课上没法真微调。真的拒绝采样、真的合理化、真的写出训练集，但「学」那一步用 **few-shot 代替了微调**，代码里就这么写，不装成微调。数据收集这半和 RLVR / GRPO 的第一步是同一件事：用可验证的奖励挑样本。学生用 haiku，因为 sonnet 一遍全对，没有提升空间也就没东西可教。

**数字**　public 12/16 → 14/16，holdout 6/8 → 7/8，数据集 14 条。52 次调用，188 秒，$0.19。

> 你在飞轮里。今晚的 prompt 是明年的训练集。

---

## 三条贯穿全课的线

1. **条条大路通 harness。** C7 自己长出来的、C8 进化出来的、A1 需要的，是同一份清单：重试、读报错、自测、上限、检查点。
2. **尺子决定形状。** C9 干净地证明了它，A3 在真模型上重现了它，B1 在一段话的尺度上也是它。
3. **自我改进是稀疏成功搜索。** C6 二十五次变异只有两次更好。所以预算是零件，不是选项。

防御三件套：公开集和私有集分开 · 每个候选固定预算 · **尺子只给评委，不给优化器**。
